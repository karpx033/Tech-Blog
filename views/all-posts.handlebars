{{#each posts as |post| }}
<div>
  <div>
    <h2>
      <a href="/post/{{post.title}}">{{post.title}}</a>
    </h2>
    <p>Created by {{post.user.username}} on</p>
  </div>
  <div>
    <p>
      {{post.body}}
    </p>
  </div>
</div>
{{/each}}